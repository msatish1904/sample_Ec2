# AWS EC2 Instance Terraform Module

This Terraform module creates an AWS EC2 instance in the specified AWS region.

## Prerequisites

- Terraform installed
- AWS CLI installed and configured

## Usage

1. Initialize Terraform:

```bash
terraform init
